name: build

on:
  push:
    branches: [ "master" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: "Login to Docker hub"
        run: docker login --username zeya10 --password dckr_pat_E6COZomTL3LJkh5_0zA5WfcrO3c
