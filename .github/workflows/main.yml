name: Build and Push Docker Image

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: سحب الكود
        uses: actions/checkout@v3

      - name: تسجيل الدخول لـ Docker Hub
        uses: docker/login-action@v3
        with:
          username: mizzzo997
          password: dckr_pat_szluJm_plE51iDVP1CDjqoBm_kk

      - name: بناء صورة Docker
        run: docker build -t zeya10/pr:latest .

      - name: رفع صورة Docker
        run: docker push zeya10/pr:latest